#pragma once
#include <iostream>
#include "common.hpp"

namespace spw {
  bool createAppKitWindow(spw::Level level, const std::string title, const std::string body) {
    std::cout << "TODO: createAppKitWindow" << (int)level << " " << title << ": " << body;
  }
}
