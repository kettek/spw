#pragma once
#include <iostream>
#include "common.hpp"

namespace spw {
  bool createWaylandWindow(spw::Level level, const std::string title, const std::string body) {
    std::cout << "TODO: createWaylandWindow" << (int)level << " " << title << ": " << body;
  }
}
