#pragma once
#include <iostream>
#include "common.hpp"

namespace spw {
  bool createX11Window(spw::Level level, const std::string title, const std::string body) {
    std::cout << "TODO: createX11Window" << (int)level << " " << title << ": " << body;
  }
}
